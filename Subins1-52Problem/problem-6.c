#include <stdio.h>
int main()
{
    int x, z, y = 0, n;
    scanf("%d", &n);
    x = n % 10;
    while(z == 0)
    {
        z = n / 10;
    }
    printf("Sum = %d\n", x+z);
    return 0;
}
